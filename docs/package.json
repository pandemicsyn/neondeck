{
  "name": "@neondeck/docs-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "astro-dev": "astro dev --host 127.0.0.1 --port 4321",
    "build": "astro build",
    "check": "astro sync",
    "deploy": "npm run build && wrangler deploy",
    "dev": "npm run build && npm run preview",
    "preview": "astro preview --host 127.0.0.1 --port 4321"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "7.2.9",
    "cookie": "^2.0.1",
    "mermaid": "^11.17.2"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.127.0"
  }
}
