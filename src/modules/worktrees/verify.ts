// Worktree-level verification mechanics remain in ../autopilot-workflows for now.
// Phase 5 documents the deferral because the current verifier is still coupled
// to prepared-diff and autopilot orchestration state.
export {};
