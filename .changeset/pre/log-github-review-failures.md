---
'neondeck': patch
---

Add production API, agent activity, dashboard connection, and runtime lifecycle logs; show underlying GitHub diagnostics; and let PR reviews continue without Checks enrichment when a fine-grained PAT cannot access the Checks API while keeping Autopilot, CI, PR comment, and watch safety gates fail-closed.
