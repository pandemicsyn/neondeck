---
'neondeck': patch
---

Move the GitHub pull request inbox to a server-managed snapshot with a three-minute refresh floor, retain the last complete queue through partial failures, and update the dashboard through the shared event stream without requiring tab switches.
