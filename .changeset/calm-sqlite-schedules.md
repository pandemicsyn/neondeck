---
'neondeck': patch
---

Run the app database in WAL mode and harden scheduler leases, workflow settlement, recovery, and SQLite diagnostics against lock contention and partial persistence failures.
