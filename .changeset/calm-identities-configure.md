---
'neondeck': minor
---

Detect incomplete Git commit identity during `neondeck init`, warn before Autopilot can inherit an OS-generated identity, and offer to configure a fail-closed global identity.
