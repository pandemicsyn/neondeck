---
'neondeck': patch
---

Keep newly started reviews visible, surface watched PR approval as a ready state, keep watch cards focused on actionable state, recover review status and recent notifications after dropped dashboard events, cleanly replace live streams and scheduler loops during development reloads, defer Flue-backed services until runtime configuration, prepare recovered Autopilot turns before admission, acknowledge briefing model refreshes only after adoption, and revision-fence learning memory changes.
